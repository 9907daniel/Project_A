spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysqldb:3306/backend?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: root
    password: projecta

  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        hbm2ddl.import_files_sql_extractor: org.hibernate.tool.hbm2ddl.MultipleLinesSqlCommandExtractor
        current_session_context_class: org.springframework.orm.hibernate5.SpringSessionContext
        default_batch_fetch_size: ${chunkSize:100}
        jdbc.batch_size: 20
        order_inserts: true
        format_sql: true

  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 376505397432-j8ic0nqg20iteq6rst945u6vto89pptu.apps.googleusercontent.com
            client-secret: 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
            scope:
              - email
              - profile

          facebook:
            client-id: 236093505939220
            client-secret: 376287e582849eb13c692a6611a36cbe
            scope:
              - email
              - public_profile

          naver:
            client-id: XpnBEneAHLi5yug35HZh
            client-secret: Kemg8bMpRT
            scope:
              - email
              - name
              - profile_name
            client-name: Naver
            authorization-grant-type: authorization_code
            #            redirect-uri: http://localhost:7072/login/oauth2/code/naver
            redirectUri: "{baseUrl}/{action}/oauth2/code/{registrationId}"

          kakao:
            client-id: 81a04e923cd64112cd7c7cc7c3e16fd1
            client-secret: ZoBarNjbUr3H73nWq8eUzcSseozA78co
            #            redirect-uri: http://localhost:7072/login/oauth2/code/kakao
            redirectUri: "{baseUrl}/{action}/oauth2/code/{registrationId}"
            authorization-grant-type: authorization_code
            client-authentication-method: POST
            client-name: Kakao
            scope:
              - profile_nickname
              - account_email

        provider:
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-name-attribute: response
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

logging:
  level:
    org.hibernate.SQL: debug

server:
  port: 7072

# cors 설정
cors:
  allowed-origins: "http://localhost:3000"
  allowed-methods: GET,POST,PUT,DELETE,OPTIONS
  allowed-headers: "*"
  max-age: 3600

# jwt secret key 설정
jwt.secret: "8sknjlO3NPTBqo319DHLNqsQAfRJEdKsETOds"

# 토큰 관련 secret Key 및 RedirectUri 설정
app:
  auth:
    tokenSecret: 926D96C90030DD58429D2751AC1BDBBC
    tokenExpiry: 1800000
    refreshTokenExpiry: 604800000
  oauth2:
    authorizedRedirectUris:
      - http://localhost:3000/oauth/redirect
